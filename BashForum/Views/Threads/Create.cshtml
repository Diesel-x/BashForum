@model BashForum.Models.Thread

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Thread</h4>
<hr />
<div class="row">
    <div class="col-md-4">

        @using (Html.BeginForm("Create", "Threads", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <label for="Category">Категория</label>
            <select class="form-control mb-3" name="Category" asp-items="@(ViewBag.Categories)">
            </select>

            <div class="form-group">
                <label for="Title" class="control-label"></label>
                <input name="Title" class="form-control" />
                <span validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="Text" class="control-label"></label>
                <input name="Text" class="form-control" />
                <span validation-for="Text" class="text-danger"></span>
            </div>

            <div class="form-group mt-3">
                <input type="submit" value="Создать" class="btn-lg btn w-100 btn-primary" />
            </div>
        }

@*        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Text" class="control-label"></label>
                <input asp-for="Text" class="form-control" />
                <span asp-validation-for="Text" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>*@
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
